# ========================================
# CONFIGURATION DU SITE WORDPRESS
# ========================================
#
# ⚠️  À FAIRE AVANT DE LANCER init-project.sh :
# 1. Copier ce fichier : cp .env.sample .env
# 2. Éditer .env et modifier les valeurs ci-dessous selon votre site
# 3. Lancer ./scripts/init-project.sh
#
# NOTE: Les credentials serveur (SERVER_HOST, SERVER_USER, SSH_KEY_PATH)
#       sont configurés dans ../../.env (fichier global à tous les projets)
#

# === DOCKER & PROJET ===
# Nom unique du projet Docker (ex: myblog, monsite-niche)
# ⚠️  DOIT être différent pour chaque site sur le même serveur
COMPOSE_PROJECT_NAME=mysite

# Port local pour le développement Docker
LOCAL_PORT=8088

# Port Docker sur le serveur de production
# ⚠️  DOIT être différent pour chaque site sur le même serveur
SERVER_PORT=8088

# === BASE DE DONNÉES ===
# Mot de passe root de la base de données
# 💡 Conseil : Générer un mot de passe fort avec : openssl rand -base64 16
DB_ROOT_PASSWORD=root

# Nom de la base de données WordPress
DB_NAME=wordpress

# Utilisateur de la base de données
DB_USER=wp

# Mot de passe de l'utilisateur de la base de données
# 💡 Conseil : Générer un mot de passe fort avec : openssl rand -base64 16
DB_PASSWORD=wp

# === ADMIN WORDPRESS ===
# Nom d'utilisateur admin WordPress
ADMIN_USER=admin

# Mot de passe admin WordPress
# ⚠️  CHANGEZ ce mot de passe pour la production !
ADMIN_PASSWORD=admin123

# Email de l'administrateur WordPress
ADMIN_EMAIL=admin@example.com

# === INFORMATIONS DU SITE ===
# Nom du site (affiché dans le header)
SITE_NAME=My Site

# Titre du site (utilisé lors de l'installation WordPress)
SITE_TITLE=My Site

# Description du site
SITE_DESCRIPTION=Mon site WordPress

# Nom pour le copyright dans le footer
SITE_COPYRIGHT=My Site

# === URLs ===
# URL locale pour le développement
# Format : http://localhost:PORT (doit correspondre à LOCAL_PORT)
SITE_URL=http://localhost:8088

# URL de production (avec HTTPS)
# Ex: https://monsite.com, https://blog.example.com
PROD_URL=https://mysite.com

# Nom du dossier sur le serveur de production
# Le site sera déployé dans /var/www/FOLDER_NAME/
# ⚠️  DOIT être différent pour chaque site sur le même serveur
FOLDER_NAME=mysite
